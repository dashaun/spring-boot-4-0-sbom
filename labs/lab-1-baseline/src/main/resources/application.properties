server.port=8081
spring.application.name=lab-1-baseline

# Production baseline: actuator on, /health and /info exposed.
# Going to prod without /actuator should be the exception, not the rule.
management.endpoints.web.exposure.include=health,info
